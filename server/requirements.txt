flask>=3.0
numpy>=1.24
argon2-cffi>=23.1     # memory-hard proof-of-work (Argon2id); falls back to SHA-256 if absent
gunicorn>=21.2        # production WSGI server used in deployment (Render start command)
