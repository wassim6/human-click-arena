# Bypass: <short name>

- **Handle:** <your github handle>
- **Date:** <yyyy-mm-dd>
- **Tool / stack:** <e.g. selenium-base UC mode + custom Bézier cursor>
- **Trace file:** `bypasses/<your-handle>-<short-name>.json`

## Score achieved

```
score:   <0.0–1.0>
verdict: <human | suspicious>
```

## Technique

What did you do to shape the motion? Be specific — this is the part that improves the detector.

- Path generation:
- Timing model:
- Tremor / noise:
- Click dwell:
- Anything else:

## Reproducibility

- [ ] Deterministic (same trace every run)
- [ ] Stochastic but consistently passes
- [ ] Passes intermittently (≈ __%)

## Why you think it works

Which sub-scores did you fool (`straightness`, `easing`, `timing`, `submovements`, `tremor`) and how?
